<%= form_for(calendar_day) do |f| %>
  <% if calendar_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_day.errors.count, "error") %> prohibited this calendar_day from being saved:</h2>

      <ul>
      <% calendar_day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>

  <div class="field">
    <%= f.label :notes %>
    <%= f.text_area :notes %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
